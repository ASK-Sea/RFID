# --- MySQL Database Configuration ---
DB_HOST=127.0.0.1          # MySQL host (localhost)
DB_USER=root               # MySQL username
DB_PASS=                   # MySQL password (empty if none)
DB_NAME=rfid_db            # Database name

# --- Backend Server Configuration ---
PORT=5000                  # Express server port

# --- MQTT Broker Configuration ---
MQTT_HOST=mqtt://127.0.0.1 # MQTT broker URL (TCP)
MQTT_PORT=1883              # MQTT port (optional if default)
MQTT_TOPIC=Dummy            # Topic your backend subscribes to

# --- Optional Settings ---
LOG_LEVEL=info              # Backend log verbosity (optional)