DB_HOST=localhost
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=your_database_name

# Optional: PORT for backend server
# PORT=5001

# Copy this file to .env and fill in real credentials before running `npm run seed`
